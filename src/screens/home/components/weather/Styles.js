import {StyleSheet} from 'react-native';
import {COLORS} from '../../../../utilities/Colors';
import {appThemeColor} from '../../../../utilities/Constants';

export const styles = StyleSheet.create({
  loaderWrapper: {flex: 1, justifyContent: 'center'},
  listWrapper: {flex: 1},
  listWeatherItem: () => {},
});
